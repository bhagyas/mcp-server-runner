---
description: 
globs: 
alwaysApply: false
---
I want to build an app that runs in the background of macOS.
It should have a mac menu bar item to show that its running.

The application lets you add commands such as 'npx @agentdeskai/browser-tools-server@latest' that run a particular server.